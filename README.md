# note_sharing_application
A secure note-sharing application built in Golang with client-side AES encryption, end-to-end encryption via Diffie-Hellman key exchange, user authentication using hashed passwords and JWT, and time-limited URL-based access control.
